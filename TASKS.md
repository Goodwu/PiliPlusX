# TASKS.md

## Now（当前推进，最多 3 条）

- [ ] 修复 macOS `BV1vY4y1N7TY` Dolby Vision 亮度不足
  - status: in_progress
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 正式 macOS 产物使用可追溯的 modern libmpv（mpv 0.41+、libplacebo/Vulkan）并完成同一 DV 输入、同一时间点的 HDR/SDR 对照；日志确认 `dolbyvision/pq` 识别、`target-peak=400`、`tone-mapping=bt.2390`、原生 EDR 可见帧和高光观感达到参考播放器。当前原正式包仍为 mpv 0.36/libplacebo disabled；本地 modern arm64 预览已验证可行，但尚未形成正式 universal 依赖。
  - latest: 已通过 `gh run download` 获取 `Goodwu/libmpv-darwin-build` 分支 `experiment/mpv-041-b3-opengl` commit `bc52bbaedf02fcf11d8042ce1e7799815a2e8405` 的成功 Actions artifact（run `34097910903`），并新增 `scripts/build_macos_goodwu_hdr.sh`、`scripts/package_macos_goodwu_mpv_hdr.sh` 固化构建。最终包 `build/macos/Build/Products/Debug/PiliPlusX-goodwu-hdr-final.app` 的 `Mpv.framework` 已确认 universal mpv 0.41.0，进程实际加载分支 libmpv、libplacebo、Vulkan、shaderc 和 FFmpeg，且同一 BVID 可播放；旧正式包与最终包均已定位到约 50% 播放位置完成画面对照，最终包高光明显恢复；已通过无 `/opt/homebrew` 绝对依赖、`codesign --verify --deep --strict`、45 项 HDR 测试和 Dart analyze。旧正式 `PiliPlusX.app` 仍嵌入 mpv 0.36.0；需要将 Goodwu artifact 依赖正式纳入发布流水线并补齐播放器日志中的 Dolby Vision/target-peak/tone-mapping 证据，任务保持 in_progress。
  - latest: architect 三轮独立审核均为 FAIL。已修复线性 RGBA16F 层的 PQ metadata 错配实验、reset 旧配置回灌、窗口屏幕 headroom、Metal command 完成状态、videoParams 决策绕过、Dart reset 事务版本和 verifier 正则；修复后源码 `flutter build macos --debug --no-pub`、45 项 HDR 测试、analyze、diff check 均通过，`PiliPlusX-goodwu-hdr-final3.app` 可打包并实际播放目标 BVID，画面不再出现已报告的立即发白现象。仍未达成：active 必须绑定成功首帧、Darwin active false 双向同步及 display refresh 顺序、完整 GL→Metal fence/lease、arm64/x86_64 modern 依赖能力一致性、同源同 PTS 光度验收；按用户要求，未将任务标记 done。
  - latest: final4 运行采样仍持续为 `bgra8Unorm`，未据此宣称 HDR 已打通；按 architect 建议将 OpenGL→Metal 生产者栅栏改为 `glFinish()`，补充 native gate 诊断字段，并修正 macOS Dolby Vision 在 display reset 后直接 return、导致 native 配置未重新下发的问题。45 项 HDR 测试、analyze、diff check、macOS debug build 通过，`PiliPlusX-goodwu-hdr-final7.app` 已重新打包；architect 复审进行中，任务保持 in_progress。
  - latest: architect 已对 reset/重叠 reset/失败重试/旧 Ready/epoch 门禁代码审查 PASS；最终源码再次通过 45 项 HDR 测试、Dart analyze、两仓库 diff check 和 `flutter build macos --debug --no-pub`，并生成 `PiliPlusX-goodwu-hdr-final8.app`（Goodwu universal mpv 0.41.0、无 `/opt/homebrew` 绝对依赖）。但产品验收仍保持 in_progress：尚未取得同一候选持续 `rgba16Float → successful frame → active=true`、reset/切屏恢复和同源同时间亮度对照证据，不能宣称整条 HDR 链路已完成。
  - latest: 在 reset 失败/重叠窗口增加共享 Future 和 reset-in-flight 门禁；architect 对最终 reset/旧 Ready 代码门禁再次 PASS。最新 `PiliPlusX-goodwu-hdr-final9.app` 已重新构建打包，Goodwu universal mpv 0.41.0 且无 `/opt/homebrew` 绝对依赖。端到端亮度验收仍未闭合：历史实测曾持续观察到 `bgra8Unorm`，当前缺少 final9 同进程持续 `rgba16Float`、active=true 和同源同时间亮度对照证据，任务保持 in_progress。
  - latest: final10 已纳入 DV 初始 hint 不伪造 PQ 的修复；46 项 HDR 测试、Dart analyze、`flutter build macos --debug --no-pub`、Goodwu universal mpv 0.41.0 打包及绝对依赖检查通过。无 HDR 显示器时仅确认到 native `rgba16Float`/EDR 候选链路，未宣称实际亮度达到 HDR；连接 HDR 显示器后仍需同源同 PTS 做高光/中灰亮度接受度对照，并完成 architect 最终复审。
  - latest: 2026-09-20 用户实际观察到目标 `BV1vY4y1N7TY` 整体偏暗。只读运行核验确认正在播放的是 `build/macos/Build/Products/Release/PiliPlusX.app`（PID 7798），其已加载 `mpv 0.36.0`，且构建标志为 `libplacebo=disabled`、`vulkan=disabled`；不是 final10 modern 候选。M27P20 的实时 EDR current=1.2、potential=10.1524。首个单变量应为同源同 PTS 的旧 Release 与 final10 对照，不在旧包上叠加全局 gamma/亮度；只有 final10 同一会话取得实际 transfer、`target-peak=400`、`bt.2390`、`rgba16Float -> successful frame -> active=true` 与高光/中灰观感证据，才可决定是否存在 producer 参数问题。
  - latest: 已生成新的可测试候选 `build/macos/Build/Products/Debug/PiliPlusX-goodwu-hdr-final11.app`。源码经 `flutter build macos --debug --no-pub` 重建后，以 Goodwu run `34097910903`（`bc52bbaedf02fcf11d8042ce1e7799815a2e8405`）的 universal archive 封装；`Mpv.framework` 为 arm64/x86_64 mpv v0.41.0，包内无 `/opt/homebrew` 绝对依赖，`codesign --verify --deep --strict`、46 项 `hdr_test.dart`、目标 Dart analyze 和 diff-check 均通过。应用 SHA-256 `78957c2f495df53cf06648b6fe990c7faca9f4563c6e8d0f4e783bfafa45d69a`，Mpv SHA-256 `44112e778650a87160028f4a9efb60e9c02cb506db5da1924605bee492205f24`；等待同源同 PTS 的用户可见亮度对照，未宣称 HDR 产品验收完成。
  - latest: final11 运行时崩溃，禁止作为亮度验收候选。`PiliPlusX-2026-09-20-232533.ips` 记录 PID 21177 在启动约 46 秒后 `SIGABRT`；final11 的实际 mpv 0.41 二进制包含并触发 `Broken API use: mpv_render_context_free() not called.`，栈为 `mp_clients_destroy -> mp_destroy_client -> abort`。当前 media-kit 的 `NativePlayer.dispose` 在 finally 中无条件延迟 5 秒调用 `mpv_terminate_destroy`，而 `NativeVideoController._dispose` 先销毁 notifier、后 detach 并可能跳过 `VideoOutputManager.Dispose`，形成 render context 尚未释放即 destroy 的 teardown 违约。Dart 首个异常未保留 stdout/stderr，故该跳过路径仍待运行证据确认；不得将其归因为 HDR 亮度、FFmpeg ABI 或仅延长延迟。修复须在 media-kit 独立范围内建立 render-context-free 完成屏障后重建候选。
  - latest: teardown 修复已进入独立审查：media-kit `NativeVideoController` 以共享 Future 串行化并发释放，先取消/排空回调，再 detach、dispose native output，等待 Darwin `VideoOutputManager.Dispose`（render-context-free barrier）后才销毁 notifier；PiliPlusX 关闭与 source-race 两条路径均先 `disposeForRebuild()`，成功后才 `Player.dispose()`，若输出释放报错则 fail-closed 而不终止 libmpv。新增两仓契约测试，PiliPlusX HDR+teardown 48 项测试、目标 analyze 和两仓 diff-check 通过；尚待 V1 独立审查、重建 final12 及真实 macOS 退出/快速重入/输出重建运行验收。
  - latest: V1 独立审查已 PASS；按反馈补齐 immutable Player/output pair、防错配释放，以及多 pair 的保留和定时重试。已生成 `build/macos/Build/Products/Debug/PiliPlusX-goodwu-hdr-final12.app`，重新编译后封装同一 Goodwu universal mpv 0.41.0（Mpv SHA-256 `44112e778650a87160028f4a9efb60e9c02cb506db5da1924605bee492205f24`），应用可执行文件 SHA-256 `c8a7e18cd78e23d456a7ac370f176b70c9e8c764957d813b0e8410cf247319ff`；codesign、无 `/opt/homebrew` 绝对依赖、48 项定向测试、目标 analyze 和两仓 diff-check 均通过。尚无 final12 实际播放退出、快速重入和输出重建证据，不能宣称崩溃或亮度验收已完成。
  - latest: final12 实际起播闪烁问题与 teardown 修复无关：用户确认其早于本轮修改；当前 PID `31758` 的 unified log 在起播时连续记录 `CAMetalLayer ignoring invalid setDrawableSize width=0 height=0`。这是 native Metal surface 在有效尺寸建立前反复提交零尺寸 drawable 的直接运行证据，且与 seek 后恢复相符；暂不把它归因于 HDR 亮度或释放逻辑。正在独立定位零尺寸的 resize/first-frame gate，未改代码。
  - latest: 已确认并修复起播闪烁/低帧率的直接结构缺陷：macOS `video_texture.dart` 的 candidate 分支和 active 分支此前会为同一 handle/generation 同时挂载两个 `PlatformViewVideo`；后一个以 `.zero` 创建并覆盖 native owner、清除 presented 状态，导致 active/recreate 循环和持续 `0×0` Metal drawable。现 macOS 在 candidate→active 全程只保留第一个原生视图，Texture 仍仅在 inactive 时作为覆盖层；Android/OHOS 路径不变。V1 独立审查 PASS，新增 macOS contract 加既有三份 media-kit contract、PiliPlusX 48 项测试、目标 analyze（media-kit 仅 3 条既有 info）和两仓 diff-check 通过。已生成 `build/macos/Build/Products/Debug/PiliPlusX-goodwu-hdr-final13.app`（应用 SHA-256 `002d9a268a7505bdc84bb5d6996a52bf829ee80c38a142d4e6affca05bf3d701`）；仍需真实冷启动验证 30 秒无需 seek、闪烁/帧率、零尺寸日志与退出重入。
  - latest: final13 用户验收确认闪烁消失、帧率初步正常，但仅在预留区域中央显示小画面。这是上一轮去重后候选层仍为 `Center > SizedBox` 的 geometry 回归：被移除的第二视图曾以 `Positioned.fill` 撑满区域。修复使 macOS 的同一候选 PlatformView 从首次挂载起直接填满既有 fitted video box，仍禁止 active 时第二次挂载；OHOS/Android 的原有居中尺寸语义不变。V1 独立审查 PASS；四份 media-kit contract、PiliPlusX 48 项测试、目标 analyze（media-kit 仅 3 条既有 info）与两仓 diff-check 通过。已生成 `build/macos/Build/Products/Debug/PiliPlusX-goodwu-hdr-final14.app`，应用 SHA-256 `9ef3b4274a9883cbd2a8511cd7880a096b5e2d94eaacd2a121eb5a147a8aaa8a`；等待真实冷启动的填充、比例、闪烁、帧率、seek、退出重入验证。
  - latest: final14 用户确认前序问题已好，但镜头平移仍有卡顿感。显示器实测为 4K@60 Hz；源码确认原生路径每帧保留 GL→Metal 正确性栅栏 `glFinish()`，并在主线程以 `waitUntilCompleted()` 等待 Metal 读完，另以非 vsync 的 60 Hz timer 触发 blit。它们是强性能候选，但没有 final14 的 source PTS、presentedTime、queue/wait 耗时或 mpv drop/avsync 指标，不能把主观卡顿直接归因于此；也不能裸删 wait（buffer pool 无 GPU reader lease）或裸删 glFinish（无等价 GL completion 信号）。下一步先以固定平移片段采集 15–30 秒基线，再仅在不规则等待得到证实时实施 lease+Metal completion 的单变量优化；若为 24fps→60Hz 规则 cadence，则不把它当作掉帧修复。当前无运行进程，未改性能代码。
  - latest: GitHub 发布流水线现以 `Publish four-platform release` 统一监听 `vMAJOR.MINOR.PATCH` tag，四个平台均成功并通过附件 manifest/SHA-256 汇总检查后才公开 Release；macOS arm64 固定 Goodwu/libmpv-darwin-build Release `v0.41.0-piliplusx.1` 的 mpv 0.41 asset SHA-256。iOS IPA 与 OHOS HAP 明确为侧载/未签名交付；已配置 Android release keystore 的四项 GitHub Secrets，缺失时仍 fail-closed。待 CI 提交推送后以预发布 tag 完成云端演练，不能将本地脚本验证视为该验收。
  - latest: 首次预发布 tag `v2.1.2-rc.1` 的 GitHub Actions 在建图阶段 0 秒失败、没有创建 job；原因是可复用 Android workflow 使用 Secrets 但未在 `workflow_call` 显式声明。现已声明四项 required Secrets；`v2.1.2-rc.2` 仍指向修复前 commit，不作为验收。待将修复连同 task/archive 记录提交后，以新 tag 复验。
  - latest: `v2.1.2-rc.3` 同样在建图阶段失败；`workflow_dispatch` 返回直接错误：reusable `ohos.yml` 的 job-level env 不允许使用 `runner.temp` context。已改为 runner 隔离的 `/tmp` 工作目录；下一个 tag 将验证该语法修复。
  - latest: `v2.1.2-rc.4` 已创建四个平台 job；Android、iOS、macOS 均在历史 `patch.ps1` 调用 `prepare_ohos_flutter.py --pointer-filter-only` 时失败，日志确认 Flutter 3.47.2 的 `scrollable.dart` 与旧 OHOS 专用转换不匹配。发布工作流现仅对这三条标准 Flutter 路径设置 `PILIPLUSX_SKIP_POINTER_FILTER_PATCH=1`；OHOS 仍保留固定 toolchain 的完整 patch 链。当前运行中的 OHOS job 不作为三平台修复结论。
  - latest: `v2.1.2-rc.5` 的 iOS job 已确认收到 pointer-filter skip 开关，却在更早的 `geetest_ios.patch` 失败：该补丁尝试新增当前源码已移除的 `geetest_plugin.dart`，其 `pubspec.yaml` 上下文也不再匹配。标准 Flutter 3.47.2 发布路径现在通过 `PILIPLUSX_SKIP_LEGACY_PATCHES=1` 跳过整套旧应用/Flutter patch 集；OHOS 不走此 PowerShell 脚本，仍保留专用工具链补丁。下一次 tag 必须验证原生编译是否还依赖该失效补丁集。
  - latest: macOS 播放器 teardown 现将同一 Player 与 VideoController 成对持有：先等待 `disposeForRebuild()` 释放 mpv render output，才销毁 Player；陈旧初始化和正常关闭都走此屏障。输出释放连续三次失败时保留 Player/output 对并延迟重试，避免重现 `mpv_render_context_free() not called` abort。`flutter test --no-pub test/plugin/pl_player/macos_teardown_contract_test.dart`（2 PASS）、`dart analyze lib/plugin/pl_player/controller.dart` 与 diff-check 通过；未替代完整 macOS 长播、重入的运行验收。
  - latest: `v2.1.2-rc.6` 的 OHOS 定向手势测试未能加载，不是断言失败：isolated copy 将 package name 改为 `piliplus` 并只重写 `lib/` 的 `package:PiliPlus/` imports，test 仍引用旧包名。`prepare_ohos_build.py` 现以同一确定性转换覆盖 `lib/` 和 `test/`；下一 tag 须在 OHOS runner 验证该回归测试与 HAP 构建。
  - latest: rc.6 的 Android/iOS/macOS 均在 native compile 失败，错误为缺少本项目所依赖的 Flutter 私有扩展 API；根因是跳过了整个 Flutter patch 集。本机固定 `d3b14c8769` 的 30 余处脏改动正是该集的预期结果，且 rc.5 Android 已成功应用该集直到最后的 OHOS-only pointer filter。现仅跳过已失效的 iOS `geetest_ios.patch`，继续在三条标准路径应用 Flutter patches，并保留 OHOS pointer-filter skip；下一个 tag 验证。
  - latest: 已生成 `PiliPlusX-goodwu-hdr-final16.app` 用于帧节奏基线。它仅在显式 `PILIPLUSX_FRAME_PACING_DIAGNOSTICS=1` 时、首个实际视频 buffer 出现后采样最多 15 秒（900 tick 上界），并只输出一条汇总：main tick、drawable 可用性、buffer 连续复用、Metal sequence gap、既有 `waitUntilCompleted()` CPU 等待及 GPU duration；默认路径不创建诊断对象。V1 审查确认未改 `glFinish`、Metal wait/present、fence 或 teardown，且无像素 readback/额外 command/semaphore。PiliPlusX 48 项定向测试、media-kit pacing contract、macOS debug build、codesign 与两仓 diff-check 均通过；final16 应用 SHA-256 `ccf054399e9b794540ec517a18e2e587624d237f43b4a5fa1ad9f0c5c1e80cac`，Mpv SHA-256 保持 `44112e778650a87160028f4a9efb60e9c02cb506db5da1924605bee492205f24`。尚待固定平移片段的真实运行汇总，未宣称卡顿已修复。
  - latest: final16 的真实平移播放已完成一轮可见验收，用户报告“没有明显卡顿”。同一进程 `/tmp/piliplusx-final16-frame-pacing.log` 的 15 秒 summary：877 tick、877/877 drawable、877/877 Metal completed、sequence `1...877`、gap=0；tick p50/p95/max=16.670/23.956/220.958 ms，CPU wait=6.174/9.700/77.466 ms，GPU=5.405/9.080/16.876 ms。单次 tick/CPU-wait 尖峰未伴随 sequence gap，且无对应可见问题；buffer 连续复用 437 次不能在缺 source PTS 下直接判作丢帧。保留正确性同步，不实施 speculative lease/fence 优化；此轮仅关闭“当前片段可见平移卡顿”的初步阻断，HDR 亮度、长播/seek/退出重入验收仍未关闭。
  - latest: 已按请求生成独立 Release 候选 `build/macos/Build/Products/Release/PiliPlusX-goodwu-hdr-final16-release.app`，不覆盖默认 `PiliPlusX.app`（后者可能仍为旧 mpv 0.36）。当前 Release 源码使用 Goodwu archive 封装的 universal `Mpv.framework`，脚本确认 mpv `0.41.0`、包内无 `/opt/homebrew` 绝对依赖；应用与 Mpv 均为 arm64+x86_64，`codesign --verify --deep --strict` 通过。应用 SHA-256 `3af05d347e8caf0b5c69b2fb6b5069daf384d55a2c3b0f3f1de074fc8f7487dc`，Mpv SHA-256 `44112e778650a87160028f4a9efb60e9c02cb506db5da1924605bee492205f24`。未提交，真实 HDR/播放验收仍待用户测试。
  - latest: 用户在 final16 Release 的 `BV11f4y1K7Wx` 报告 P1 8K 无图、切换其它分辨率仍无图，以及切 P2 后先显示旧的 8K/缺杜比、切一次才刷新。直接运行日志有两类独立证据：(1) mpv 对 AV1 报 `Your platform doesn't support hardware accelerated AV1 decoding`、`Failed to get pixel format` 与 OBU parse 错误，8K AV1 不能作为当前 macOS 的可用输出宣称；(2) 01:26:53 画质点击链在 `VideoDetailController.findVideoByQa` 的空 `videoList.first` 抛出 `Bad state: No element`。根因是画质菜单 `Obx` 只观察 `currentVideoQa`，`data` 在分 P1→P2 切换而 qn 不变时不是响应式依赖，继续捕获 P1 `supportFormats`；点击旧 quality 会先更新当前 qn、再在 P2 DASH 查找不存在的条目并异常，播放器不重建。需修复为：以当前 DASH tracks 作为菜单唯一数据源/用播放数据 revision 触发重建，选择前验证 qn，失败时保留现有播放并提示；另外对无可解码 codec 的 8K AV1 明确禁用或降至实际可用的非 AV1 rendition。当前仅诊断，未改代码。
  - latest: 已实现 macOS 8K 解码门禁与菜单修复，未提交。Dart 以独立 `probeVideoDecode` 请求每个当前 DASH 的 8K codec/尺寸，macOS 使用 Apple 面向远端编码选择的 `VTIsHardwareDecodeSupported` 查询；结果按播放数据 revision 缓存，尺寸无效、未知 codec、探测中、失败或不支持时仅 8K fail-closed，4K 及以下不受影响。画质选项按当前 DASH tracks 过滤并显式观察 revision；HDR 与 8K 都保持可点击的视觉置灰项并返回对应提示。选择事务先复核当前 tracks/revision/codec 资格，`findVideoByQa` 空集改为可处理失败。默认 8K 在探测未完成时回退至最高非 8K track。新增 eligibility 单测，目标 analyze（仅一条既有 async-context info）和 diff-check 通过；完整 app build 与 `BV11f4y1K7Wx` 实机复验仍待，不能宣称产品验收完成。
  - latest: 复核后移除了错误的“无 codec configuration record 时创建 VT session”判定：它会把本机 AVC/HEVC/AV1 全部误判为不可用，不能作为 DASH 能力门禁。实现现严格使用 Apple 为远端编码选择提供的 `VTIsHardwareDecodeSupported(codec)`，仍返回并按 codec/width/height 缓存结构化结果；尺寸无效和未知 codec fail-closed。当前 macOS 直接运行该 API：AVC、HEVC、AV1 均为 true；这只是本机 VideoToolbox codec 能力，不替代 `BV11f4y1K7Wx` 的 mpv 实际输出验收。新纯函数测试覆盖 P1→P2 同 qn 的 DASH menu 替换、pending/failed/unsupported 8K、4K AV1 软解、任一 codec 可解和 channel codec/尺寸传参；与 HDR 契约共 53 项 PASS。目标 analyze 仅剩既有 `header_control.dart:807` info，Swift 完整 typecheck（含生成注册器 stub）和 diff-check PASS。任务仍待完整 app build 与真实 P1/P2/8K/4K 运行验收。

  - latest: final17 真实 P1 AV1 8K 后黑帧并记录 `Your platform doesn't support hardware accelerated AV1 decoding` / `Failed to get pixel format`；单独的系统 VideoToolbox codec-family=true 不足。直接查询 final18 随包 `libavcodec` 的公开 `avcodec_get_hw_config`：H.264/HEVC 有 VideoToolbox config，AV1 为空，且该 FFmpeg configure 未启用 `av1_videotoolbox`。原生 probe 现先动态检查 app 同一 `libavcodec.dylib` 中目标 decoder 的 VideoToolbox config，再交叉系统硬件结论；缺任一结论均只对对应 8K fail-closed，4K 仍允许软解。Swift typecheck、53 项 Dart tests、diff-check 和 current-source final18 Goodwu universal mpv 0.41 codesign 均通过；final18 GUI 曾仍进入 AV1 黑帧，诊断候选重启后无法取得可操作窗口，故必须补做 native 回传及 P1/P2/4K 的真实验收，不能宣称完成。
  - latest: 又收紧了冷启动事务：当前播放数据 revision 的 8K probe 必须完成，才计算 `targetVideoQa`、codec 与首个 URL；后台 probe 不再能与默认 8K preference 并发启动旧 AV1 source。revision 在等待期间变化或 controller 已关闭即不再提交。53 项 Dart tests、Swift typecheck、diff-check 通过；`PiliPlusX-goodwu-hdr-final20-8k-transaction-gate.app` 从当前源码构建，Goodwu universal mpv 0.41、无 Homebrew 绝对依赖且 codesign 通过。CUA 启动 final20 时目标进程存在但未返回可枚举 CG window，因此本轮仍没有替代真实 P1/P2/8K/4K 可见验收的证据。
  - latest: 为排除同 bundle-id 的 default Debug 干扰，已关闭两个由本轮测试启动的 PiliPlusX 进程，随后只让 final20 存活；CUA 仍返回 `cgWindowNotFound`，故当前环境没有可用的 UI target。作为原生路径的直接二进制验证，final20 内嵌 `libavcodec` 的公开 `avcodec_get_hw_config` 实测为 `h264=true`、`hevc=true`、`av1=false`（VideoToolbox device type 6）。这证明 gate 的 native 依赖判断，不证明用户可见菜单/播放结果；同一 UI 阻塞已连续出现，等待可用窗口或外部显示状态恢复后再完成真实验收。
  - latest: 再次审计发现冷启动仍可能绕过多 codec 8K gate：菜单 transaction 会从 AV1/HEVC 同档中筛出已确认硬解的 HEVC，但旧初始 URL 路径会按 `supportFormats` 偏好重新挑回 AV1。现初始 URL 改为复用 `findVideoByQa(... requireConfirmedEightKHardware: true)`，从而与菜单使用同一已验证 track 选择；若没有合格 track，保留 fail-closed 而不生成 URL。新增纯函数回归证明 AV1-first/HEVC-confirmed 的 8K 输入只保留 HEVC；eligibility+HDR 定向测试现为 54 PASS，目标 analyze 仅有既有 `header_control.dart:807` async-context info，`MainFlutterWindow.swift` typecheck 与 diff-check 通过。未启动新 GUI；用户运行的默认 app 未被关闭。默认 Debug app 包含先前运行/重签名遗留的无效嵌套签名，不用它充当本轮构建或产品验收证据。
  - latest: final21 独立候选（Goodwu universal mpv 0.41、codesign 通过）以唯一 bundle id 启动后，`BV11f4y1K7Wx` P1 仍为黑帧；当前 `~/.pili_logs.json` 直接记录其 AV1 `Failed to get pixel format`、`Get current frame error` 与无硬解报错，故默认非 8K 回退尚未真实达成。验证工具按窗口显示名 `PiliPlusX` 解析时错误另启默认 Debug 包；已逐一核验路径并关闭 final21 与该误启的默认 Debug PID，现无 PiliPlusX 进程。后续不得再以显示名/CUA 自动解析启动候选；需采用明确路径且确认不会派生默认包后才恢复 runtime 验收。
  - correction: 上一条“final21 P1 黑帧”的归属不成立。final21 启动后，自动化按显示名 `PiliPlusX` 调用 `getApp` 实际另启并绑定默认 `build/.../PiliPlusX.app`；截图、交互和随后 AV1 日志都来自这个误启动的默认进程，而非 final21。已核验并关闭 final21 PID `34065` 与误启动默认 PID `34144`，当前无 PiliPlusX 进程。final21 仅有构建/封装/签名证据，仍没有可信 UI runtime verdict；后续不得再以 display name/CUA 自动解析启动候选。
  - latest: 初始 URL 复用 `findVideoByQa` 后仍读取未赋值的 `late currentDecodeFormats`，会在首次 DASH source selection 抛初始化异常，阻断任何 8K fail-closed 回退。现将它初始化为安全 AVC 偏好；后续仍会按可用 track/用户 preferCodecs 选择更优 codec。54 项 eligibility+HDR 测试、目标 analyze（仅既有 async-context info）和 diff-check 通过；`flutter build macos --debug --no-pub` 通过。已生成但未启动 `PiliPlusX-goodwu-hdr-final22-8k-initial-codec-gate.app`，Goodwu universal mpv 0.41、codesign、无 Homebrew absolute dependency 均通过，app SHA-256 `f17a37e197aa1b811330dbc7a811cf0e8824b8c1128022cd5560cd0f5f96edc7`。P1/P2 真实运行门保持未通过。
  - latest: 为安全恢复 GUI 验收，final22 测试产物改为唯一 bundle id，并由系统清单确认仅 `PiliPlusX Final22 Gate` 在运行、默认 `PiliPlusX.app` 未启动；但 CUA 拒绝以该唯一显示名绑定。未尝试任何回退显示名，已关闭 final22 PID `38779`，当前无 PiliPlusX 进程。该环境限制仍使 P1/P2 用户可见门无法取证；不能用候选进程存在或系统清单替代 runtime 验收。
  - latest: 已用**完整 final22 `.app` 绝对路径**成功绑定唯一 `PiliPlusX Final22 Gate`（PID `39352`），没有启动 default Debug 包。目标 `BV11f4y1K7Wx` 的 P1 已实际起播并连续可见出图（控件显示 `1080P`；说明文本确认 P1=8K、P2=4K 杜比视界），因此修正后的首次 DASH 选源不再因 `currentDecodeFormats` 未初始化而中止。该候选中 CUA 在 Flutter 播放控件每帧重建后持续返回 `AXError.failure` / stale element，无法可靠打开画质或选集菜单，故没有把 P1 8K 禁用 toast、4K 选择或 P2 菜单刷新伪报为通过。已只关闭 PID `39352`；`ps` 确认当前没有任何 `PiliPlusX` 进程。定向 eligibility+HDR 测试为 54 PASS，目标 analyze 仅既有 `header_control.dart:807` info，P1/P2 菜单可见验收继续待可用 UI 自动化或人工操作。
  - latest: 冷启动 8K 回退改为共用纯函数 `highestEligibleNonEightKTrack`，避免代码路径重分叉；它按相同 HDR/8K eligibility 从当前 DASH 返回最高的非 8K track。新增回归覆盖“AV1 8K 被拒绝时仍回退 AV1 4K（而非 1080P）”；eligibility+HDR 定向测试现 55 PASS，目标 analyze 仍仅既有 `header_control.dart:807` info，diff-check PASS。
  - latest: final22 的唯一 bundle-id 隔离导致未登录，已停止使用该候选。当前源码重新 `flutter build macos --debug --no-pub` 后封装 final24：`PiliPlusX-goodwu-hdr-final24-8k-original-identity.app`，保持 `com.example.piliplusx` 原 identity、Goodwu universal mpv 0.41、无 Homebrew absolute dependency、codesign 通过，binary SHA-256 `0cbff367139801293419b0bb00dbd616467ce41d1718be3d8e9893305eb184f3`。只通过完整 absolute path 启动 PID `44148`，系统中没有其他 PiliPlusX；CUA 明确读到登录态（99+、已关注）。`BV11f4y1K7Wx` P1 已连续可见出图，控件与用户亲自打开的画质菜单均显示 8K/4K/1080P/720P/480P/360P，证明当前 P1 DASH menu 未遗留 P2 项，且同档可确认硬解 codec 的 8K 路径可播放。窗口保持给用户检查；未擅自点击 P2/切档，P2 的杜比显示及菜单替换仍待。
  - latest: final24 P1 在重新导航后曾短暂黑屏，但本次新鲜日志只含 CDN `https: Stream ends prematurely`，没有新增 AV1 hwdec/pixel-format 错误，故不能将该网络输入失败归因于 8K gate。随后同一 P1 回到连续可见画面且仍为 8K。CUA 的 control-bar AX node 会在动画帧间失效：语义“选集”错误落入页面标签、已恢复目标视频；“下一集”节点 stale，未实际切 P。停止猜测坐标，窗口保留单一 final24 进程；P2 menu runtime gate 保持待验证。
  - latest: 用户在唯一登录态 final24 中手动选择 P2 并打开分辨率菜单，确认杜比视界出现、8K 不残留；随后 CUA 只读截图也显示 P2 实际连续出图且控制条为 `4K`。这与 current-DASH/revision 模型一致，完成此前唯一缺失的 P1→P2 可见验收。P1 同档可确认硬解 codec 的 8K 路径、P2 的 current-DASH 菜单替换、4K 软解不受 8K gate、失败不抛 `No element` 的实现和定向回归均已具备；进行最后静态复核后结束本项。
  - latest: 用户明确要求“如需运行时回退则门禁不正确”。已撤回未构建的 post-open `hwdec-current` 回退实验；final24 亦在该实验前构建，候选与当前源码都只在**前置门禁拒绝**时为默认 8K 偏好选择最高非 8K track。一旦 gate 放行 8K，就保持同一已验证 codec，不会在运行时降档。前置条件仍是：当前实际 8K track 的 codec/尺寸、随包 FFmpeg 的 VideoToolbox decoder config、`VTIsHardwareDecodeSupported` 三者必须同时为真；缺任一项 fail-closed。最终定向 tests 55 PASS、Swift typecheck/diff-check PASS；target analyze 仅既有 `header_control.dart:807` info。

- [ ] 补齐 controller source/output 完整时序覆盖
  - status: in_progress
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 以可控依赖覆盖 source 切换、复用 Player open、codec probe、output rebuild、非末引用释放和最终 dispose 的真实调用链；证明无旧源回写、永久 in-flight、失效输出发布或共享监听丢失。2026-09-17 已新增 opaque-handle lifecycle orchestrator，生产 `Player.open`/listener rebind 已接入同一 open gate；44 项 HDR 定向测试覆盖 probe/output 迟到释放、重叠 open 与 final-dispose 顺序。初始 `_initPlayer`、rebuild 和 controller 最终释放尚未完整改由该编排器执行，任务继续进行。

- [ ] 构建唯一最终候选并完成 macOS、Android 与 OHOS 功能验收矩阵
  - status: in_progress
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 最终候选绑定源码、依赖、补丁、各平台产物摘要和脚本版本。OHOS：横屏及竖屏视频各 3 个完整周期、控制条/手势/seek 边界、surface 重建、同进程重入、DV/PQ/SDR 和同源颜色对照分别有实机证据，DV 长周期完成 30 个连续周期。macOS：同一 DV 输入/PTS 的 HDR/SDR 高光与中灰对照、持续 `rgba16Float → successful frame → active=true`、reset/切屏恢复和核心播放操作有运行证据。Android：arm64 候选在目标 Android 环境完成安装、冷启动、首页/搜索/详情/播放首帧和控制操作验证，且测试窗口内无 ANR/崩溃；HDR 输出另按具备能力的真机独立验收。设备重连后推荐列表专项已通过；DV surface 脚本的连续播放基线通过，但冷重启后未重新打开视频，故只记录冷启动控制，不计 surface 恢复通过。P13 已完成横屏 DV 三轮矩阵，P14 已完成同一进程页面退出与目标播放重入；仍缺最终候选上的竖屏 SDR/PQ、surface 重建、HLG/HDR Vivid 与同源颜色对照。
  - latest: Architect review 后已将 Flutter OHOS engine embedding 的生产差异整理为 `tool/ohos/flutter_embedding/ohos_hcpp_embedding.patch`，native NAPI 与 embedding tests 分别独立为 opt-in patch；`build_sign_hap_test.sh` 的 engine inline substitutions 已迁移到 `scripts/prepare_ohos_embedding.py`。三个 patch 对当前 `dev:/home/wuweiwei1/tools/flutter-ohos` dirty checkout reverse-check 通过，并在固定 `aa76d9bbeee7806a87dbd202d2550dfd11550b82` 临时 clean worktree 中全部 clean-apply 通过（生产 4、native 1、test 1 个路径）。随后将 `build_sign_hap_test.sh` 的 debug-only HCPP marker gate 与 release 路径分开，避免 release HAP 因 debug 诊断字符串缺失而误失败；CI HAR 消费和 native `libflutter.so` 来源契约仍未验证，因此不宣称供应链闭环。
  - latest: Android ANR 已在 arm64 模拟器的连续退出/重进场景验证修复。旧候选在固定 `BV1T7t96BECu` 的 `tap(375,330)` 可重现 `InputDispatcher` 5 秒超时，主线程停在 `libmpv.so mpv_set_property_string`。修复使默认 `PlayerConfiguration.async=true` 的公开 `Player.setProperty` 走异步协议，同时将 native `Player` 的进程级互斥锁收窄为实例锁，避免上一页异步释放阻塞下一页独立 libmpv context 创建；`on_load/on_unload` 钩子内的属性直写保留同步，避免事件处理等待自身异步回复。Android SDR/Texture 的 app HDR 参数写入仍跳过；“听视频”保持仅播放音轨、不播放视频。最新 arm64 APK（SHA-256 `82db6375c0086772144516e4370f7ef33d947df2cb3820b7b9cc8c7e19eb9139`）完成严格 5 轮：每轮 BVID 身份、首帧/帧推进、触摸后 6 秒及退出后重入均 PASS。脚本现保存启动时的 `dumpsys window` ANR 基线，只把本轮新增 window 记录或清空后 logcat 的新 ANR 判为失败，避免历史系统残留误判。artifact：`/tmp/piliplusx-android-emulator-20260920/reentry-anr-instance-lock-strict-5cycles-20260920-220147`；`hdr_test.dart` 46 项、目标 analyze、build、bash-n 和 diff-check 均通过。
  - latest: Android 返回键候选已排除并回退：模拟器上的系统返回手势可正常离开视频页，故不修改 `PopScope`；此前现象归为模拟器物理 Back 输入/映射问题，而非已证实的播放器路由问题。回退后的 arm64 APK 已用 `scripts/build_android_arm64_release.sh` 构建，并成功覆盖安装到 `emulator-5554` 与 Android 实机 `WJX5T17314001484`（Huawei VKY-AL00，arm64-v8a，Android 9）。
  - latest: Huawei Android 9 实机的播放日志已定位为 `OMX.hisi.video.decoder.avc` 直通 `mediacodec` 在 `gpu-next`/Texture 输出协商中请求 10 个输出缓冲、而驱动上限为 4；首次失败后同一解码器可重试进入 Executing，但会向用户显示“打开解码器错误”。按 Android 版本而非厂商定向：仅 Android API 28 及以下且没有持久化硬解偏好的首次默认值改为 `mediacodec-copy,auto-safe`，已选择的硬解模式及 API 29+ 默认值不变。`hdr_test.dart` 46 项、相关 `dart analyze`、`bash -n` 和 diff-check 已通过；arm64 APK 已于 2026-09-20 22:58:24 成功覆盖安装到该实机，待以原触发视频采集新鲜 logcat 判定运行结果。
  - latest: 复测新视频已确认 `mediacodec-copy,auto-safe` 生效，Hisi AVC 解码器进入 `onStart`，没有 `invalid buffer count`、`Failed to allocate buffers` 或 codec error。其前仍有 `OMXNodeInstance setConfig(..., 0x6f700006) ERROR: BadParameter(0x80001005)`：该索引不在公开 AOSP OMX 名称表，且相同华为系统扩展调用在其它应用/音频解码器中也会失败后继续播放；AOSP 仅透传厂商 `OMX_SetConfig` 返回值。故它是旧固件/Hisi 组件不接受可选厂商配置的非阻断系统日志，应用不能也不应拦截或通过关闭硬解“修复”。后续只将其与 `MediaCodec` codec error、输出缓冲分配失败、无帧或用户可见失败联合作为阻断信号。

## 已完成（近期）

- [x] 构建 Android arm64 APK 并完成模拟器首轮验收
  - status: done
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 仅生成 arm64 APK，确认包内 ABI、签名和模拟器安装/启动结果。
  - latest: 2026-09-20 生成 `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`（SHA-256 `8c611b54e8a33acad4b0834e778346139cea25b32bddf03974649629a6938cb1`），包内仅有 `lib/arm64-v8a/`，v2 Debug 签名有效。`bluekey-api36-arm64`（API 36）安装、冷启动和首屏绘制通过；随后发生持续 `Input dispatching timed out` ANR，Wait 后仍未恢复，故不能视为模拟器交互运行通过。完整证据见 context 与 `docs/status/platform-sdr-build-status.md`。

## 当前交付边界与验收规则

- 当前交付主线是 macOS、Android 与 OHOS 的播放器功能验收闭环：控制条和手势、全屏、持续播放、HDR 颜色亮度及 source/output 生命周期。三平台共用改动必须分别保留可归因的构建与运行证据；不以任一平台的通过替代另一平台验收。
- 保留现有原生 HDR 路线。不得以 Texture SDR 回退、屏蔽功能、降低验收条件或删除诊断解决 HDR 路径上的交互问题。变更技术路线前，记录直接阻断证据、替代路线的最小可行性实验和受影响回归范围；影响交付范围的决定交由用户。
- 每个平台同一时段只推进一个关键阻断项；可并行处理不改变当前候选包的独立测试或证据提取。Android 模拟器、macOS 本机与 OHOS 实体机的输入执行者和证据目录必须隔离。实验开始前记录假设、基线、唯一主要变量、预期区分结果和停止条件；同类实验连续两轮未缩小范围时重新诊断。
- 设备、权限、依赖或环境缺失时，完成独立工作后标记 blocked 并停止相关验收；明确区分产品失败、测试基础设施失败、环境阻塞、证据不足和通过。验证器故障先修验证器；不得以重复探测、静态检查、构建通过、HAP 安装、单次动作或跨试次拼接日志代替实体机验收。
- 每个候选 HAP 必须绑定源码、依赖、补丁版本、包摘要、设备、脚本版本和关键参数；保留最近可复现通过基线。只有相关实现、依赖、环境或验收标准改变时才重新打开既有通过项，并注明失效原因。
- 实体机操作只通过 HDC 脚本；同一设备同一时段只能有一个输入执行者。动作前校验目标应用、PID、attachment、逻辑全屏状态和新鲜布局；控制条隐藏时最多一次安全唤醒后重抓布局。前置不成立立即 fail-closed，不追加点击。
- 每个输入试次必须绑定唯一 ID、坐标、时间范围、PID/view/epoch 和独立消费的日志标记。不得跨试次拼接 pointer、callback 和 request，也不得让迟到日志改写已结束试次的 verdict。四标记只证明请求链路；完整全屏验收还须证明事务提交、正确方向和持续出帧。
- 验证器须覆盖旧布局、错误 PID、过期 attachment、重复/乱序/缺失/迟到日志的离线反例；诊断日志默认关闭且有界。不得仅扩大超时，先定位事件生成、输出、传输或解析的首个缺口。
- 手势以累计约 18px 确认方向，只有 `|dx| > 3 × |dy|` 的明显横向手势进入 seek；分别验证播放器内手势、倾斜容错、Cancel/多指和播放器外推荐列表滚动。无直接反例不得继续调整阈值、层序、代理按钮、合成 PointerUp 或 down 直接全屏。
- 全屏状态仅在平台事务成功后提交；竖屏视频全屏不强制旋转。取消、销毁和失败不得伪造成功或永久阻塞后续请求。所有异步 Player/source/output 完成后的发布都校验 source、Player、输出事务和存活状态；失效结果不发布但须释放资源及 in-flight 标记。非末引用释放不得取消共享监听；最终释放必须等待真实释放；controller 测试覆盖真实调用链的 open、probe、rebuild 和 dispose 时序。
- 应用只决定 HDR 策略，media-kit 管理输出配置，OHOS VO 管理动态 NativeWindow 色彩契约；不得多层重复写动态输出属性。颜色验收必须使用同设备、同源、同一暂停帧或可复现片段；日志色彩契约或不同播放时刻截图不能单独证明显示正确。HLG/HDR Vivid 没有可靠样片时保持待办，不阻塞已定义的 DV、PQ、SDR 验收。

## Next（近期候选）

- [ ] P13 竖屏 SDR/Texture 三轮矩阵（中断后重跑）
  - status: todo
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 以 P13 HAP `/Users/wuweiwei1/Downloads/PiliPlusX-ohos-final-candidate-20260918-p13-signed.hap`（SHA-256 `8b2dca2ebdccfe26174ffba75eeb7ab6c51926a55faeb406735493647738831c`）在 `BV1sA4y1D7ZA` 执行 `--vertical --cycles 3`；必须取得绑定 PID/view/epoch 的 initial enter 与 cycle-1/2/3 exit/re-enter 共七份 action record PASS、三轮 portrait、播放帧推进和 final drain。2026-09-18 已启动但被用户中断；仅生成 `01-home.json`、`02-search.json` 和 Hilog，尚未打开视频或进入动作阶段，不形成通过或失败结论。

- [ ] 在获得可靠样片时验证 HLG/HDR Vivid
  - status: todo
  - context: archives/conversations/player-architecture-remediation.md
  - acceptance: 先确认实际源格式，再进行独立实体机验收；没有可靠样片时维持待办

## Blocked（等待输入或外部条件）

- 当前无同进程退出重入阻塞项。

## 规则

- 新任务必须写入本文件；完成任务必须打勾。
- 每个活跃任务必须有唯一 context、可验证 acceptance 和明确状态：todo / in_progress / blocked / done。
- 同一总目标和其重复子项不得同时作为活跃进度；因候选版本或验收口径变化重新打开的任务必须写明失效原因。
- 每次实质推进更新本文件和当前 conversation；已完成工作及其证据只保留在对应 conversation，不在本文件复述。
